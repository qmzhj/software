# 校园通讯平台（最小可运行版本）

## 功能说明

本版本实现了用户注册、登录、登出功能。

- 注册：输入学号/工号、姓名、密码；系统自动判定学生或教职工角色。
- 登录：密码错误5次后锁定30分钟；登录成功获取 Token。
- 登出：清除本地 Token，返回登录页。

### 5.2日更新:
- 数据库中存储师生信息的数据结构users增加registed项，用于表示是否已注册
- 用户信息更改为从users.json通过函数读入数据库
- 注册逻辑增加检查，检查数据库中是否有该学生学号，以及学号和姓名是否匹配，因为预设是从学习获取学生信息，所以学生学号和姓名的映射是已知的
- 登录增加检查是否注册，若未注册则返回错误:账号未注册！
### 5.8日更新:
- 数据库中存储师生信息的数据结构users增加phone项，用于存储师生的手机号码
- 修改个人设置页面，显示姓名学号角色手机号码，其中角色姓名学号不可修改，手机号码和密码可修改
- 为登录页面增加"忘记密码?"功能，通过向用户手机号发送短信验证来修改密码
- 忘记密码功能暂时还无法使用，会莫名显示“验证码错误”
- 注册界面手机号输入框还未配备相应功能，只有前端页面
### 5.13日更新:
- 忘记密码功能修复完成
- 为注册界面的手机号输入框配备相应的后端逻辑
### 5.19
- 检查之前实现的聊天功能的逻辑，正在修复群组成员数显示错误，考虑自动更新功能的设计
- 自动更新功能描述:每隔一段时间从学校数据库中同步数据，并调用相应函数，将对应用户加入对应群组
### 5.20
#### 进行的改动
- 修复前端群组成员显示错误，因为规则确定，所以使用动态计算成员数量的方法，减少空间开销，增加了计算开销
#### 想法
- 考虑实现自动创建课程群组功能，需要的数据为某课程包含的教职工uid
- 课程数据结构设计:课程名，课程uid，课程包含的所有学生的uid，上课时间，上课地点，课程教师uid
- 考虑完善群组创建功能，可以在创建时勾选关联聊天室
### 想法
- 可以通过关键词查找联系人和群聊
- 用户可自行创建聊天分组，默认创建的分组为课程群，即时聊天，私聊与群组
- 公告跳转到对应官网
- 课程安排为自己的课程表，信息通过教务系统获取
- 空闲教室为未上课的教师
- 可以查询食堂图书馆的空闲情况
- 可以查询参加赛事的人员
- 个人设置增加简介
- 公告合并进校园查询
## 运行环境

- Windows 11 家庭中文版（或其他 OS）
- Python 3.12.3 及以上

## 安装依赖

打开命令行（cmd 或 PowerShell），进入项目目录，执行：
pip install flask

## 启动服务

在项目目录执行：
python app.py

看到 `Running on http://0.0.0.0:5000` 后，打开浏览器访问 `http://localhost:5000`。

## 使用方法

1. 在“注册”标签页输入学号/工号、姓名、密码进行注册。
2. 切换到“登录”标签页，用刚注册的账号登录。
3. 登录成功后显示主界面，点击“登出”返回。