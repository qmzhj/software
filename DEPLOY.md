# 校园通 — 部署文档

## 环境要求

- Python 3.10+
- 操作系统：Windows / Linux / macOS 均可
- 依赖：仅 Flask（其余均为 Python 标准库）

## 快速开始

```bash
# 1. 进入项目目录
cd hw

# 2. 安装依赖
pip install -r requirements.txt

# 3. 启动服务
python app.py

# 4. 打开浏览器访问
http://localhost:5000
```

服务启动后，`campus.db` 数据库会自动创建并初始化（如果不存在）。

## 默认账户

系统启动时从 `users.json` 预载用户信息，常用测试账号：

| 学号 | 姓名 | 角色 |
|------|------|------|
| TE2023009 | 庞雄文 | 教师 |
| 202421324906 | 梁瑞政 | 学生 |
| 202421326577 | 曹云龙 | 学生 |
| 202421324917 | 林枫灿 | 学生 |
| 202421326351 | 陈欣 | 学生 |
| 202421324910 | 陈琳 | 学生 |
| 202421327009 | 刘谦 | 学生 |
| 202421326580 | 段蔚瑄 | 学生 |
| 202421324921 | 王博 | 学生 |

**已注册的测试账号密码统一为 Test1234 **。

未注册账号：`202421327117` 王娜，更多账号请见 `users.json`。

## 注意事项

- **不要移除 `threaded=True`**：`app.run(..., threaded=True)` 是 SSE 长连接的必要条件，移除会导致实时推送阻塞。
- **Token 存储在内存**：服务重启后所有登录会话失效，用户需重新登录。
- **重置数据库**：删除 `campus.db` 后重启服务即可自动重建 clean 数据库。
