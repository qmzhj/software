- 课程数据结构设计为数据库中的两个变量:
lesson：包含课程名，课程uid，上课时间，上课地点，课程教师uid
lesson_stu:包含课程uid，与课程uid相对应的学生uid

- 在app.py中实现课程数据读入的工具函数，功能为从lessons.json中读入课程数据，自动创建课程uid，随后插入到数据库中
- lessons.json中的数据格式为：
```
[
  {
    "lesson_name": "数据结构",
    "teacher_uid":"......",(教师uid)
    "schedule_weekday":"星期三",(表示在星期几上课)
    "schedule_period":"1-2" (表示上课时间为第1，2节课)
    "schedule_weeks":"1-16"(表示在1-16周有课)
    "location":"北504",
    "stu_uids": [202421326577,202421326578,......]
  },
  ......
]
```

- 更新ensure_default_groups函数，使其根据课程数据，自动创建该用户所在的课程的聊天室，并且自动创建课程群组，课程群组关联所有该用户所在的课程聊天室
- 更新get_user_groups函数，增加一条动态课程群组聊天室数量的计算逻辑，就像动态计算私聊群组和即时聊天群组成员数量那样
- 帮我在当前目录下创建lessons.json文件，并生成几组测试数据