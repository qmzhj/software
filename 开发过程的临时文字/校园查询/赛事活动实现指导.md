# 赛事活动模块实现指导

## 概述

基于 `5_31校园查询初步设计.md` 中"赛事活动"章节的设计，实现赛事详情展示、小组管理（创建/加入/退出/满员）、管理员发布赛事等功能。

整体分为三个阶段，按顺序执行。

---

## 步骤一：赛事详情 API + 前端详情视图

### 1.1 后端：赛事详情 API

在 `app.py` 中新增路由，返回单个赛事的完整信息及其下的小组列表。

**新增路由：**

```python
@app.route('/api/events/<int:event_id>', methods=['GET'])
@token_required
def get_event_detail(event_id):
    conn = get_db()
    event = conn.execute('SELECT * FROM events WHERE id=?', (event_id,)).fetchone()
    if not event:
        return jsonify({'error': '赛事不存在'}), 404
    groups = conn.execute('''SELECT eg.*, u.name as leader_name,
        (SELECT COUNT(*) FROM events_groups_members WHERE group_id=eg.id) as member_count
        FROM events_groups eg JOIN users u ON eg.leader_uid=u.uid
        WHERE eg.event_id=? ORDER BY eg.created_at''', (event_id,)).fetchall()
    # 当前用户所在小组
    uid = g.current_user['uid']
    my_group = conn.execute('''SELECT egm.group_id FROM events_groups_members egm
        JOIN events_groups eg ON egm.group_id=eg.id
        WHERE eg.event_id=? AND egm.member_uid=?''', (event_id, uid)).fetchone()
    conn.close()
    return jsonify({
        'id': event['id'],
        'name': event['name'],
        'type': event['type'],
        'start_time': event['start_time'],
        'end_time': event['end_time'],
        'location': event['location'],
        'target': event['target'],
        'organizer': event['organizer'],
        'description': event['description'],
        'creator_uid': event['creator_uid'],
        'groups': [{
            'id': g['id'],
            'group_name': g['group_name'],
            'leader_uid': g['leader_uid'],
            'leader_name': g['leader_name'],
            'status': g['status'],
            'max_members': g['max_members'],
            'description': g['description'],
            'member_count': g['member_count'],
            'created_at': g['created_at']
        } for g in groups],
        'my_group_id': my_group['group_id'] if my_group else None
    })
```

### 1.2 前端：赛事详情视图

当前 `query.html` 的赛事标签以卡片列表展示赛事，需点击后切换为赛事详情视图，替代原有的弹窗方式。

**修改 `loadEvents()` 中的点击事件**，改为调用 `showEventDetail(eid)` 隐藏列表、显示详情视图。

**页面结构**：
- `#eventListView`：赛事卡片列表容器（已有的）
- `#eventDetailView`：赛事详情容器（新增，默认隐藏）
- 详情视图顶部有"← 返回赛事列表"按钮

**赛事详情视图内容**：
- 赛事基本信息（名称、类型、起止时间、地点、面向对象、主办方、详细介绍）
- 赛事参与人栏目：
  - 搜索框（按学号搜索用户所在小组）+ "显示/不显示满员小组" 开关
  - 小组列表，每个小组卡片显示：
    - 小组名、组长名、成员数/上限、状态徽标（招募中/已满员）
    - 操作按钮：
      - 未加入任何小组 → "申请加入"按钮
      - 当前用户是组长 → "设置满员"按钮
      - 当前用户是该小组成员（非组长） → "退出小组"按钮
  - "创建小组"按钮

**实现要点**：
- 详情容器使用 `overflow-y: auto` 处理长内容
- 小组状态用不同颜色区分：招募中=绿色，已满员=灰色

---

## 步骤二：后端小组管理 API

### 2.1 创建小组

```python
@app.route('/api/events/<int:event_id>/groups', methods=['POST'])
@token_required
def create_event_group(event_id):
    uid = g.current_user['uid']
    data = request.get_json()
    group_name = data.get('group_name', '').strip()
    if not group_name:
        return jsonify({'error': '小组名称不能为空'}), 400
    max_members = data.get('max_members', 4)
    if not isinstance(max_members, int) or max_members < 2 or max_members > 20:
        return jsonify({'error': '人数上限需为2-20的整数'}), 400
    conn = get_db()
    # 检查用户是否已在该赛事有小组
    existing = conn.execute('''SELECT 1 FROM events_groups_members egm
        JOIN events_groups eg ON egm.group_id=eg.id
        WHERE eg.event_id=? AND egm.member_uid=?''', (event_id, uid)).fetchone()
    if existing:
        conn.close()
        return jsonify({'error': '你已在该赛事的小组中，不能创建新小组'}), 400
    c = conn.cursor()
    c.execute('INSERT INTO events_groups (event_id, group_name, leader_uid, max_members, description) VALUES (?,?,?,?,?)',
              (event_id, group_name, uid, max_members, data.get('description', '')))
    gid = c.lastrowid
    c.execute('INSERT INTO events_groups_members (group_id, member_uid) VALUES (?,?)', (gid, uid))
    conn.commit()
    conn.close()
    return jsonify({'message': '小组创建成功', 'group_id': gid}), 201
```

### 2.2 查看小组列表（含搜索和满员过滤）

复用步骤一的 `GET /api/events/<id>/groups` 或在现有赛事详情中直接返回小组列表。搜索和满员过滤在**前端**实现：
- 前端从详情 API 拿到全部小组后，用 JS 按条件过滤
- `show_full` 开关：`filter(g => g.status !== 'full' || showFull)`
- 搜索成员：用输入的学号调用搜索 API

### 2.3 搜索成员所在小组

```python
@app.route('/api/events/groups/search', methods=['GET'])
@token_required
def search_event_group_member():
    eid = request.args.get('eid', type=int)
    uid = request.args.get('uid', '').strip()
    if not eid or not uid:
        return jsonify({'error': '参数不足'}), 400
    conn = get_db()
    group = conn.execute('''SELECT eg.*, u.name as leader_name,
        (SELECT COUNT(*) FROM events_groups_members WHERE group_id=eg.id) as member_count
        FROM events_groups eg
        JOIN events_groups_members egm ON eg.id = egm.group_id
        JOIN users u ON eg.leader_uid=u.uid
        WHERE eg.event_id=? AND egm.member_uid=?''', (eid, uid)).fetchone()
    conn.close()
    if group:
        return jsonify({'found': True, 'group': {
            'id': group['id'], 'group_name': group['group_name'],
            'leader_name': group['leader_name'], 'status': group['status'],
            'member_count': group['member_count'],
            'max_members': group['max_members']
        }})
    return jsonify({'found': False})
```

### 2.4 设置满员

```python
@app.route('/api/events/groups/<int:gid>/status', methods=['PUT'])
@token_required
def set_event_group_status(gid):
    uid = g.current_user['uid']
    data = request.get_json()
    new_status = data.get('status', 'full')
    if new_status not in ('recruiting', 'full'):
        return jsonify({'error': '无效的状态值'}), 400
    conn = get_db()
    group = conn.execute('SELECT * FROM events_groups WHERE id=?', (gid,)).fetchone()
    if not group:
        return jsonify({'error': '小组不存在'}), 404
    if group['leader_uid'] != uid:
        return jsonify({'error': '仅组长可修改状态'}), 403
    conn.execute('UPDATE events_groups SET status=? WHERE id=?', (new_status, gid))
    conn.commit()
    conn.close()
    return jsonify({'message': f'状态已更新为 {new_status}'})
```

### 2.5 申请加入小组

```python
@app.route('/api/events/groups/<int:gid>/apply', methods=['POST'])
@token_required
def apply_join_event_group(gid):
    uid = g.current_user['uid']
    data = request.get_json()
    message = data.get('message', '') or '我想加入你的小组'
    conn = get_db()
    group = conn.execute('''SELECT eg.*, e.name as event_name FROM events_groups eg
        JOIN events e ON eg.event_id=e.id WHERE eg.id=?''', (gid,)).fetchone()
    if not group:
        return jsonify({'error': '小组不存在'}), 404
    if group['status'] == 'full':
        return jsonify({'error': '该小组已满员'}), 400
    # 检查是否已在小组中
    existing = conn.execute('SELECT 1 FROM events_groups_members WHERE group_id=? AND member_uid=?', (gid, uid)).fetchone()
    if existing:
        return jsonify({'error': '你已在该小组中'}), 400
    # 发送通知给组长
    now = time.time()
    conn.execute("INSERT INTO notifications (type, sender_uid, receiver_uid, content, extra, status, created_at) VALUES (?,?,?,?,?,'pending',?)",
                 ('group_join', uid, group['leader_uid'], message,
                  json.dumps({'group_id': gid, 'group_name': group['group_name'], 'event_name': group['event_name']}),
                  now))
    conn.commit()
    conn.close()
    return jsonify({'message': '申请已发送，等待组长审批'})
```

### 2.6 审批/拒绝加入申请

在现有 `POST /api/notifications/<notif_id>/approve` 和 `reject` 端点中新增 `group_join` type 分支：

```python
# 在 approve 处理分支中新增：
if notif['type'] == 'group_join':
    extra = json.loads(notif['extra'])
    gid = extra['group_id']
    # 检查小组是否已满员
    cur = conn.execute('SELECT status, max_members FROM events_groups WHERE id=?', (gid,)).fetchone()
    if cur['status'] == 'full':
        return jsonify({'error': '该小组已满员'}), 400
    cur_count = conn.execute('SELECT COUNT(*) FROM events_groups_members WHERE group_id=?', (gid,)).fetchone()[0]
    if cur_count >= cur['max_members']:
        conn.execute('UPDATE events_groups SET status=? WHERE id=?', ('full', gid))
        return jsonify({'error': '小组人数已满，已自动设为满员'}), 400
    conn.execute('INSERT INTO events_groups_members (group_id, member_uid) VALUES (?,?)',
                 (gid, notif['sender_uid']))

# 在 reject 处理分支中新增：
if notif['type'] == 'group_join':
    # 无需额外操作，通知状态已更新为 rejected
    pass
```

### 2.7 退出小组

```python
@app.route('/api/events/groups/<int:gid>/members/<member_uid>', methods=['DELETE'])
@token_required
def leave_event_group(gid, member_uid):
    uid = g.current_user['uid']
    conn = get_db()
    group = conn.execute('SELECT * FROM events_groups WHERE id=?', (gid,)).fetchone()
    if not group:
        return jsonify({'error': '小组不存在'}), 404
    # 只允许自己退出或被组长踢出
    if member_uid != uid:
        if group['leader_uid'] != uid:
            return jsonify({'error': '无权操作'}), 403
    if member_uid == group['leader_uid']:
        return jsonify({'error': '组长不能退出，请先转让组长或解散小组'}), 400
    conn.execute('DELETE FROM events_groups_members WHERE group_id=? AND member_uid=?', (gid, member_uid))
    conn.commit()
    conn.close()
    return jsonify({'message': '已退出小组'})
```

### 2.8 管理员发布赛事

```python
@app.route('/api/events', methods=['POST'])
@token_required
def create_event():
    if g.current_user['role'] != 'manager':
        return jsonify({'error': '仅管理员可发布赛事'}), 403
    data = request.get_json()
    conn = get_db()
    c = conn.cursor()
    c.execute('''INSERT INTO events (name, type, start_time, end_time, location, target, organizer, description, creator_uid)
                 VALUES (?,?,?,?,?,?,?,?,?)''',
              (data['name'], data.get('type',''), data.get('start_time',''),
               data.get('end_time',''), data.get('location',''), data.get('target',''),
               data.get('organizer',''), data.get('description',''), g.current_user['uid']))
    eid = c.lastrowid
    # 自动创建赛事聊天室
    chat_id = 'chat_event_' + str(eid)
    now = time.time()
    c.execute('INSERT OR IGNORE INTO groups_chat VALUES (?,?,?,?,?,?)',
              (chat_id, data['name'] + '聊天室', 'system', now, 'system', '赛事'))
    c.execute('INSERT OR IGNORE INTO group_members VALUES (?,?,?,?,?)',
              (chat_id, g.current_user['uid'], 'admin', now, 0))
    conn.commit()
    conn.close()
    return jsonify({'message': '赛事发布成功', 'event_id': eid}), 201
```

---

## 步骤三：前端小组管理交互

### 3.1 小组列表渲染

在 `query.html` 的赛事详情视图中，增加小组列表渲染区。小组数据从详情 API 获取。每个小组卡片包含：

```html
<div class="group-card" style="border:1px solid #e2e8f0;border-radius:10px;padding:12px;margin-bottom:10px;">
  <div style="display:flex;justify-content:space-between;align-items:start;">
    <div>
      <strong>${group.group_name}</strong>
      <span style="font-size:12px;padding:2px 8px;border-radius:10px;background:${statusColor};color:white;margin-left:8px;">${statusText}</span>
    </div>
    <span style="font-size:13px;color:#64748b;">👤 ${group.member_count}/${group.max_members}</span>
  </div>
  <div style="font-size:13px;color:#64748b;margin-top:4px;">👨‍👩‍👧‍👦 组长：${group.leader_name}</div>
  <div style="font-size:13px;color:#475569;margin-top:4px;">${group.description}</div>
  <div style="margin-top:8px;display:flex;gap:6px;" id="groupActions_${group.id}"></div>
</div>
```

### 3.2 按钮逻辑

根据当前用户的参与状态显示不同按钮：

| 用户状态 | 显示按钮 |
|---------|---------|
| 未加入任何小组，该小组招募中 | "申请加入" |
| 未加入任何小组，该小组已满员 | 无按钮（灰色"已满员"文字） |
| 当前用户是该小组组长 | "设置满员/招募中" |
| 当前用户是该小组成员（非组长） | "退出小组" |
| 当前用户在其他小组 | 无按钮（提示"你已在其他小组"） |

### 3.3 搜索和满员过滤

在详情视图顶部添加：
- 搜索框：输入学号后调用 `/api/events/groups/search?eid=<id>&uid=<学号>`，搜索结果高亮展示
- 开关：`显示已满员小组` 复选框，默认勾选

### 3.4 创建小组弹窗

```html
<div class="modal-overlay" id="createGroupModal">
  <div class="modal" style="width:400px;">
    <h3>👥 创建小组</h3>
    <input id="newGroupName" placeholder="小组名称" />
    <input id="newGroupMaxMembers" type="number" value="4" min="2" max="20" style="width:100%;padding:10px;border:1px solid #e2e8f0;border-radius:8px;margin-bottom:8px;" />
    <textarea id="newGroupDesc" placeholder="小组简介/招募要求..." style="width:100%;padding:10px;border:1px solid #e2e8f0;border-radius:8px;min-height:80px;font-family:inherit;"></textarea>
    <div class="btn-row">
      <button class="btn-cancel" onclick="closeModal('createGroupModal')">取消</button>
      <button class="btn-primary" onclick="submitCreateGroup()">创建</button>
    </div>
  </div>
</div>
```

---

## 执行顺序

```
步骤一（后端详情 API + 前端详情视图）
    ↓
步骤二（后端小组管理 API）
    ↓
步骤三（前端小组管理交互）
```

- 步骤一完成后：赛事可点击查看详情，但小组功能不可用
- 步骤二完成后：后端 API 就绪，可用 curl 测试
- 步骤三完成后：完整的前端交互可用

API 路由汇总：

| 方法 | 路由 | 说明 |
|------|------|------|
| GET | `/api/events/<id>` | 赛事详情（含小组列表） |
| POST | `/api/events/<id>/groups` | 创建小组 |
| GET | `/api/events/groups/search?eid=&uid=` | 搜索成员所在小组 |
| PUT | `/api/events/groups/<gid>/status` | 设置满员/招募中 |
| POST | `/api/events/groups/<gid>/apply` | 申请加入 |
| DELETE | `/api/events/groups/<gid>/members/<uid>` | 退出小组 |
| POST | `/api/events` | 管理员发布赛事 |

---

## 六月四日讨论：小组管理交互改进方案

### 当前存在的问题

1. **按钮显示逻辑错误**：非组长的用户也显示了"设置满员"按钮，应为仅组长可见
2. **缺少退出按钮**：小组成员无法退出小组（后端已实现但前端未挂载按钮）
3. **缺少成员列表查看**：无法查看小组内具体有哪些成员
4. **组长管理能力不足**：组长无法踢出组员、转让组长、解散小组
5. **组长无法退出/解散**：组长卡在自己的小组中无法脱身

### 改进方案

#### 小组卡片显示

每张小组卡片显示基本信息 + 快捷操作按钮：

- **所有卡片都显示**：小组名称、组长姓名、成员数/上限、状态徽标、"查看成员"按钮
- **未加入任何小组，该小组招募中** → 直接显示"申请加入"按钮
- **未加入任何小组，该小组已满员** → 显示灰色"已满员"文字
- **当前用户是该小组组长** → 直接显示"设置满员/招募中"按钮
- **当前用户是该小组成员（非组长）** → 直接显示"退出小组"按钮
- **当前用户已在其他小组** → 不显示快捷操作按钮

#### 成员弹窗（点击"查看成员"后弹出）

弹窗内容：小组成员列表（姓名+学号），底部按用户角色显示额外操作按钮：

| 当前用户状态 | 弹窗内显示的操作按钮 |
|-------------|-------------------|
| 该小组的组长 | 解散小组、踢出组员（每个非组长成员旁）、转让组长 |
| 该小组成员（非组长） | 退出小组（与卡片上重复，弹窗内可再放一份） |
| 其他情况 | 仅查看，无额外操作按钮 |

注意：申请加入、设置满员/招募中、退出小组这些高频操作已在卡片上直接提供，弹窗主要解决的是**查看成员列表+组长管理操作**（踢人、转让、解散）。

#### 需新增的后端 API

| 方法 | 路由 | 说明 |
|------|------|------|
| GET | `/api/events/groups/<gid>/members` | 获取小组成员列表（含姓名、学号、角色） |
| POST | `/api/events/groups/<gid>/kick` | 踢出成员（仅组长可用） |
| POST | `/api/events/groups/<gid>/transfer` | 转让组长（仅组长可用） |
| POST | `/api/events/groups/<gid>/dissolve` | 解散小组（仅组长可用，自动清空成员，发送通知） |

#### 实现要点

- 踢出/转让/解散后需同步更新 `events_groups_members`、`group_members`（聊天室）、`user_group_chats`（赛事群组）三张表
- 退出小组或踢出成员后，小组低于上限则自动恢复为 `recruiting` 状态
- 解散小组：删除 `events_groups` 记录、清空 `events_groups_members`、从聊天室移除所有成员、**给每个成员发送一条 `system` 通知**（通知内容如"小组「xxx」已解散"）
- 转让组长：更新 `events_groups.leader_uid`，原组长降为普通成员 |
